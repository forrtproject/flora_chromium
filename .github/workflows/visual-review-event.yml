name: Visual review event
on:
  pull_request_review:
    types: [submitted, dismissed, edited]
permissions:
  contents: read
jobs:
  notify:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Re-evaluate visual approval in the trusted workflow_run job.'
